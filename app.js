fetch("data.json")
  .then((response) => response.json())
  .then((data) => {
    // data.forEach(function (d) {
    console.log(data);

    data.forEach(element => {
      
      
    });
    // });
  });
